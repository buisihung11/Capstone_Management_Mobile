import 'package:flutter/material.dart';

class TestScreeb extends StatelessWidget {
  const TestScreeb({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Hung BUi"),
    );
  }
}
